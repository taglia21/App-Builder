nixpkgs = ['python311', 'postgresql']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements-railway.txt']

[start]
cmd = "python -m uvicorn src.dashboard.app:create_app --factory --host 0.0.0.0 --port $PORT"
